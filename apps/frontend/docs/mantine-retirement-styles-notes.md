# Mantine retirement — styles notes

Running log of the Tailwind/CSS written while replacing Mantine components, so we can spot repeated patterns and decide what to **elevate** to the global Tailwind config (`tailwind.config.ts`) or a shared `@layer components` in `globals.css`. All existing `--illinois-*` / `--navbar-*` / `--foreground*` CSS variables are reused verbatim, so colors match 1:1 across the migration.

## Elevation candidates (review before Phase 3)

| Pattern                                                                                                                                                        | Where it appears                                         | Suggested home                                                                             |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| **Shine sweep on hover** (diagonal white gradient swept via `::after`, 650ms)                                                                                  | `AuthMenu` avatar                                        | `@layer components { .shine-on-hover { … } }` in globals.css, or a Tailwind plugin utility |
| **Illinois-orange outline button** (`h-9 min-w-[100px] border border-[--illinois-orange] text-[--illinois-orange] hover:bg-[rgb(255_95_5_/_0.05)]`)            | `AuthMenu` sign-in, `GlobalHeader` nav links + menu icon | shared `<Button variant="illinoisOutline">` or a `.btn-illinois-outline` component class   |
| **Navbar link** (`text-[13px] font-medium text-[--navbar-foreground] hover:text-[--navbar-hover] hover:bg-[--navbar-hover-background] data-[active=true]:...`) | `Navbar` desktop + mobile links                          | `.navbar-link` component class                                                             |
| **montserratHeading font** (`${montserrat_heading.variable} font-montserratHeading`)                                                                           | everywhere in chrome                                     | already a shared pattern; consider a `<Heading>`/`font-heading` utility                    |

## Per-component log

### Slice 1 — navbar chrome + static pages (2026-07-01)

> ⚠️ **Next 16 `next/font` weight gotcha (found via VR against production).** Under Next 16, `montserrat_heading` (700) and `montserrat_paragraph` (500) both resolve to family **`Montserrat`** with _both_ faces loaded — so `font-montserratHeading` no longer forces bold; the element's own `font-weight` now selects the face. Production (older Next) had a **700-only** `__Montserrat_*` heading family, so nav labels (whose CSS is `font-weight:500`, confirmed in the original `GlobalHeader` `link` createStyles) rendered at **700 anyway**. Net effect: any `font-montserratHeading` element with a non-700 weight renders **lighter** than production. Slice-1 nav buttons (`orangeOutlineBtn`, `navLinkClass`, AuthMenu sign-in) were `font-medium` → now **`font-bold`** so they render Montserrat 700 like production. **This is a Next-16 (wave-3c) side effect that affects heading text app-wide — audit heading weights against prod in every later slice.**

**AuthMenu.tsx** — `Menu`→`DropdownMenu`, `Avatar`(gradient)→shadcn `Avatar`/`AvatarFallback`.

- Gradient fill: `bg-[linear-gradient(135deg,var(--illinois-industrial),var(--illinois-blue))]` on `AvatarFallback`.
- **Shine sweep (ELEVATE):** `relative overflow-hidden after:absolute after:inset-0 after:content-[''] after:-translate-x-full after:bg-[linear-gradient(120deg,transparent_0%,transparent_30%,rgba(255,255,255,0.2)_50%,transparent_70%,transparent_100%)] after:[transition:transform_650ms] hover:after:translate-x-full` (use the explicit `[transition:…]` shorthand, not `after:duration-[650ms]` — the latter is ambiguous with tailwindcss-animate and emits no CSS) + `hover:-translate-y-px hover:shadow-[0_4px_12px_rgba(0,0,0,0.15)]`. → candidate `.shine-on-hover`.
- Dropdown: `rounded-xl border border-[--border] bg-[--background] p-1.5 shadow-[0_8px_24px_rgba(0,0,0,0.2)]`; items `rounded-lg px-4 py-2.5 text-sm font-medium text-[--foreground] focus:bg-[--muted]`.
- **Sign-in button = orange-outline button (ELEVATE):** `h-[2.2rem] min-w-[100px] rounded-md border border-[--illinois-orange] px-3 text-sm font-medium text-[--illinois-orange] hover:bg-[rgb(255_95_5_/_0.05)]`.

**GlobalHeader.tsx** — dropped `createStyles`; hoisted two Tailwind consts:

- `orangeOutlineBtn` (same orange-outline button as AuthMenu sign-in; adds `focus:outline focus:outline-2 focus:outline-[--dashboard-button]`, `bg-white`) — used for nav links.
- `orangeIconBtn` — square `h-[2.2rem] w-[2.2rem] p-1` variant for the hamburger.
- Both duplicate AuthMenu's sign-in style → **ELEVATE to one shared button/variant.**
- The mobile-dropdown `<style jsx>` (menuSlideDown/Up, .menu-item, pulse) is NOT Mantine — left as-is.

**Navbar.tsx** — `Flex`→div, `Burger`→`IconMenu2`/`IconX` toggle button, `Transition`+`Paper`→conditional render w/ `animate-in fade-in-0 zoom-in-95 origin-top-right duration-200`, `useDisclosure`→`useState`.

- **`navLinkClass` (ELEVATE `.navbar-link`):** `flex items-center justify-center gap-[0.4rem] rounded px-3 py-2.5 text-[13px] font-medium text-[--navbar-foreground] hover:bg-[--navbar-hover-background] hover:text-[--navbar-hover] data-[active=true]:bg-[--navbar-background] data-[active=true]:text-[--navbar-active]` + mobile `max-md:*` overrides. Mantine tokens resolved: spacing.xs=10px→py-2.5, spacing.sm=12px→px-3, spacing.lg=20px→py-5, radius.sm=4px→rounded.
- Mobile dropdown: `absolute right-2 top-16 z-[2] w-[calc(100%-1rem)] max-w-[330px] rounded-[10px] border border-[--navbar-border] bg-[--background-faded] shadow-lg lg:hidden`.

**cropwizard-licenses.tsx** — `Title order={2}`→`<h2 className="${montserrat_heading.variable} font-montserratHeading text-[2.2rem] font-bold leading-[1.35]">`, `Flex`→`flex min-h-[50px] flex-col flex-wrap items-start justify-start gap-4` (gap="md"=16px), `Text`→`<p>`, `List`/`List.Item`→`<ul className="list-disc pl-10">`/`<li>`.

> ⚠️ **CRITICAL — Title uses the app's THEME override, not Mantine defaults.** `apps/frontend/src/pages/_app.tsx` `MantineProvider.theme.headings` sets `fontFamily: 'Montserrat, Roboto, sans-serif'` and `sizes: { h1: 2.125rem→**3rem**, h2: 30px→**2.2rem** }`. So `<Title order={2}>` renders at **35.2px (2.2rem) in Montserrat 700, line-height 1.35 (→47.52px)** — NOT the 1.625rem/26px library default. Verified 1:1 against chat.illinois.edu computed styles. **Any `<Title>`→`<h2>` migration must apply `font-montserratHeading` + the theme size, and be diffed against production.** ELEVATE a `<Heading>` component / `.title-*` scale: h1 3rem, h2 2.2rem, both Montserrat 700 lh 1.35 (h3+ still Mantine defaults — measure before migrating).

**disclaimer / terms / privacy** — removed dead/unused `@mantine/core` import lines only (no rendered Mantine).

**Deferred from slice 1:** `index.tsx` (home — `Button`×5/`Card`×4/`Image`, gradient + polymorphic `component="a"`; own careful pass) and `newsletter-unsubscribe.tsx` (uses `notifications.show` directly → needs the sonner foundation).

### Batch 2 (Step 3) — UploadNotification, CitationCard, index.tsx

**index.tsx** — the deferred slice-1 file, now converted. `Card`/`Button` → shadcn.

- The two polymorphic-prop cases the plan called out: `Button component="a"` (docs link, blue banner) → plain `<a>` styled with `buttonVariants({ className: '...' })` from `button.tsx`; `Card component="a"` (course-card grid) → `next/link`'s `<Link>` with the card's border/radius/overflow/colors carried over manually as `card.tsx` exports no variants function. `Card.Section` → plain `<div>`s (pure layout dividers, no extra Mantine styling to replicate).
- Hero CTA button (`variant="light"` + inline `backgroundColor`/`color` override) → `bg-(--illinois-orange) text-(--illinois-white) hover:bg-(--illinois-orange)/90 rounded-sm` — the Mantine `variant` was cosmetically irrelevant since the inline style already fully overrode colors; fidelity bar applied, no slot-by-slot translation needed.
- Three `<Button>` blocks in this file are dead code inside `{/* JSX comments */}` (never parsed/rendered) — left untouched.

**UploadNotification.tsx** — `Card`(padding=0)→plain `<div>` (Paper-style bordered container, not the shadcn `Card` primitive, since padding/gap are fully custom via className and the default Card's `--card-spacing` would fight it). `Text`→`<p>`/`<pre>`. `Button variant="subtle" compact`→`<Button variant="ghost" size="icon-sm">`. Three `<Tooltip label classNames={{tooltip}}>` wrapping a non-interactive child → `<Tooltip><TooltipTrigger render={<span/>}>…</TooltipTrigger><TooltipContent className="...">…</TooltipContent></Tooltip>`.

**CitationCard.tsx** — `UnstyledButton`→`<button type="button">`, `Paper`→plain bordered `<div>`, `Text`→`<p>`, `Badge` (radius="sm" size="xs")→shadcn `Badge` with `h-4 rounded-sm px-1 text-[10px]` override (shadcn's default is a `h-5 rounded-4xl` pill, visually different from Mantine's small square badge — hand-translated since the default doesn't match). `Image`→plain `<img>` (NOT `next/image`): the `src` is a runtime S3 presigned URL or an arbitrary third-party favicon URL, neither of which is next/image-eligible without broadening `next.config`'s remote-image allowlist app-wide — out of scope for this file, flagged here per the plan's "close enough" + "don't invent new problems" spirit. Dead `Tooltip` import (unused) removed.

**ChatNavbar.tsx** — dropped `createStyles`/`useStyles` (its breakpoints varied by `isAdminOrOwner`, which CSS media queries can't express, so this was already computing effective visibility in JS via a tracked `windowWidth` state). Ported every `theme.fn.smallerThan/largerThan(isAdmin ? X : Y)` slot to a plain JS boolean (`showBurger`, `showNewChatLink`, `showSettingsLink`, `showAdminDashboardLink`) compared against the existing `windowWidth` state, then used those booleans to conditionally render (rather than CSS-hide) the nav buttons — functionally identical, simpler than reproducing per-instance media queries in Tailwind. The `.link` class's narrow-viewport `list-item` compact variant was **dropped**: it only ever showed in a sliver of widths between two already-independent hide thresholds and wasn't visually load-bearing. The `&[data-active="true"]` sub-rule on `.link` was dead code — nothing in the file ever set that attribute — and was dropped outright. `Burger`→`Button variant="ghost" size="icon-sm"` + manual `IconMenu2`/`IconX` swap (only rendered when `showBurger`, matching `Navbar.tsx`'s existing idiom). `Transition`(pop-top-right)+`Paper`→conditional `{opened && <div className="animate-in fade-in-0 zoom-in-95 ...">}`, `border-(--background-dark) bg-(--background-faded) text-(--foreground)`. `Flex`/`Group`/`Container`→plain `<div>`s with the equivalent `flex`/`gap`/`justify` utilities. Gave the toggle button `aria-label="Toggle burger menu"` and the dropdown `data-testid="hamburger-menu"` to satisfy `ChatNavbar.test.tsx`'s pre-existing `vi.mock('@mantine/core', …)` stub shape (which mocked `Burger`/`Transition` with exactly those two strings) — the mock itself is now dead code (nothing in the component imports `@mantine/core` anymore) but was left in place since removing it wasn't required to get the suite green.

**NavigationSidebar.tsx** — `createStyles`(theme-token slots, `&.collapsed`/`&:hover`/`&[data-active="true"]` combinators)→dropped entirely; every slot rewritten as a Tailwind class string, with `isOpen`/`isCollapsed` booleans branching into **mutually exclusive** class sets per property (e.g. `isCollapsed ? 'gap-0 p-3' : 'gap-3 px-4 py-3'`) rather than concatenating a base class with a same-property override — two same-specificity unprefixed Tailwind utility classes touching the same CSS property have no reliable "last one wins" guarantee from plain string concatenation (unlike component props run through `cn()`/`twMerge`, which resolves such conflicts explicitly). `&[data-active="true"]` (real usage here, unlike ChatNavbar) → Tailwind's `data-[active=true]:` variant. The dead `classes.navText` slot (defined but never referenced in JSX — the actual collapse-hide for nav labels happens via an inline `md:${isCollapsed ? 'hidden':'inline'}` at the call site) was dropped, not ported. `theme.radius.md/lg`→`rounded-(--radius-md)`/`rounded-(--radius-lg)`, `theme.shadows.sm/md`→Tailwind's built-in `shadow-sm`/`shadow-md` (no matching CSS var exists; fidelity bar applied).

**Sidebar.tsx** — only `Button` and `Tooltip` remained (rest of the file was already shadcn/Tailwind). `Tooltip`(`label`, `disabled`, `withArrow`, `position="right"`, `withinPortal`)→`Tooltip disabled={…}` + `TooltipTrigger render={<div .../>}` (Base UI's `disabled` prop is native on `Tooltip.Root`, so the Mantine `disabled` prop ports 1:1) + `TooltipContent side="right"` (arrow renders by default, matching `withArrow`; Base UI always portals, matching `withinPortal`). The large `<div role="button">` that was the Tooltip's single child became the `render` element (all its props moved there), with its actual visible content (banner, name/description, settings button) becoming `TooltipTrigger`'s children — the Base UI `render={<El/>}` pattern injects trigger props onto `El` and renders `children` inside it, so this is a structural no-op, not a visual change.

**APIRequestBuilder.tsx** — the 9 `styles`/`sx` slots, evaluated one by one:

1. Language `Select` `input`/`dropdown`/`item`/`rightSection` — **mostly dropped, kept where it mattered.** `--dashboard-button` (focus border) equals `--ring`/`--illinois-blue` in light mode, and shadcn's `SelectContent` `bg-popover`/`text-popover-foreground` already resolves to the same white/`--illinois-blue` as Mantine's `dropdown`/`item` — took the defaults. The one real difference: default `SelectItem` hover uses `--accent` (Illinois blue-industrial, a distinct color), not Mantine's subtle `--foreground-faded` — hand-translated via `data-highlighted:bg-(--foreground-faded) data-highlighted:text-(--foreground)` on each `SelectItem`/`ComboboxItem`. `rightSection` custom chevron dropped — the components already render their own.
2. Model `Select` (searchable) — same resolution as #1, now via the new `combobox.tsx` wrapper (grouped by provider using `Combobox.Group`/`Combobox.Collection`; `ComboboxCollection` added to the shared wrapper since grouping requires it).
3. System Prompt `Textarea` `input` — dropped. Default `border-input`/ambient text color already matches `--foreground-faded`/`--foreground` closely enough; `--dashboard-button` focus border already equals default `--ring` in light mode.
4. User Query `Textarea` `input` — same as #3, dropped.
5. Temperature `Slider` `track`/`bar`/`thumb`/`label` — **hand-translated.** Default `bg-muted`/`bg-primary` resolve to `--illinois-storm-95`/`--illinois-orange`, visibly different from the wanted `--foreground-dark`/`--dashboard-button`. Added `trackClassName`/`indicatorClassName`/`thumbClassName` override props to the shared `slider.tsx` (same pattern as the existing `marks`/`markLabelClassName` props). The floating value `label` on the thumb has no shadcn equivalent — replaced with a small badge next to the slider, mirroring the pattern already used in `Chat/Temperature.tsx`.
6. Retrieval Only `Switch` `track`/`label` — **dropped**, switched to `variant="labeled"` + the built-in `label`/`tooltip` props instead of hand-styling: its checked-state colors (`--dashboard-button`) already match exactly.
7. Retrieval Only `Tooltip` `tooltip` (bg/color) — **dropped.** Switch's built-in tooltip prop renders via the shared `--tooltip`/`--tooltip-background` vars (dark bg/light text, the app-wide tooltip convention) instead of Mantine's inverted light-bg/dark-text one-off; accepted as close enough for a low-stakes info icon rather than adding a per-instance override point to `switch.tsx`.
8. Stream Response `Switch` `track`/`label` — same resolution as #6.
9. Code snippet `Textarea` `input` (color/monospace) — **kept the monospace requirement** (`font-mono`, real default difference) but dropped the explicit `color` override since the app-wide `body` ambient-color fix (this batch's other change) now makes the default inherited text color resolve correctly.

Also removed a dead a11y `useEffect` that patched `aria-label` onto `[role=combobox]`/`[role=slider]` post-render (a Mantine-era wrapper-div workaround) — Base UI's `SelectTrigger`/`ComboboxInput`/`Slider` all accept `aria-label`/`getAriaLabel` directly, so the labels are now correct by construction. Deleted a dead, entirely-unused `const styles = {...}` object (never referenced anywhere in the file).

**ModelSelect.tsx (Chat/)** — the searchable `Select` (grouped by provider, custom `itemComponent`, custom max-height clamped to the settings-modal boundary) → the new `combobox.tsx` wrapper. Grouping uses Base UI's `items: {value, items}[]` shape + `<ComboboxGroup items={group.items}><ComboboxGroupLabel/><ComboboxCollection>{...}</ComboboxCollection></ComboboxGroup>` (mirrors the Base UI docs' "Grouped" recipe). `isItemEqualToValue` compares by `.value` since item objects are rebuilt every render. The custom `constrainDropdownHeight`/`getModelDropdownMaxHeight`/`getModelDropdownBoundaryRect` boundary-clamping logic is preserved as-is and applied via an inline `style={{ maxHeight: '<px>px' }}` on `ComboboxContent` (which now wins over the wrapper's default `max-h-(--available-height)` utility class); `toRemScaledDropdownHeight` (a Mantine-`rem()`-specific compensation) is no longer called from the component but is left in place since it's still directly unit-tested. `withinPortal`/`zIndex={400}` on the two country-of-concern tooltips and the dropdown itself have no direct equivalent — left at Base UI's default (always-portaled, `z-50`); flagging for a real-browser check since the plan notes 400-vs-50 stacking may need hand resolution if the settings modal visually covers the dropdown. `ModelItem`'s Group/Text → flex `<div>`/`<span>`, its Tooltip → the standard `Tooltip`/`TooltipTrigger render={<span/>}`/`TooltipContent` idiom. The "Model" heading has a _dynamic_ order (4 or 5 depending on `isSmallScreen`), so it's `role="heading" aria-level={...}` on a `<div>` rather than a literal `<h4>`/`<h5>`, since JSX can't conditionally choose a tag name as cleanly — paired with `.heading-h4`/`.heading-h5` classes for sizing.

**Ingest form family** (`WebsiteIngestForm.tsx`, `WebScrape.tsx`, `GitHubIngestForm.tsx`, `CanvasIngestForm.tsx`, `MITIngestForm.tsx`) — a shared idiom was applied consistently across all five rather than converting each independently, since they're near-duplicate card+dialog+URL-input layouts:

- `Card` (the clickable dialog-trigger card, no Card subcomponents used, fully custom border/radius/bg via className) → plain `<div>`, not the shadcn `Card` primitive — its base classes would fight the fully custom styling already present. `Text`→`<p>`.
- Mantine `Input`'s `icon` prop (no shadcn equivalent) → a `relative` wrapper div with the icon `absolute`-positioned left (`pointer-events-none`, `-translate-y-1/2`) and the `Input` given `pl-11`. Used for every URL/search input in this family.
- `styles={{ input: {...}, wrapper: {...} }}` slots (color/background/border/focus-border, all already pointing at `--foreground`/`--background-faded`/`--background-dark`/`--illinois-orange`) → dropped in favor of the shadcn `Input`'s own classes; only the color vars were carried over as Tailwind arbitrary-value classes (`border-(--background-dark) bg-(--background-faded) text-(--foreground) focus-visible:border-(--illinois-orange)`), no slot-by-slot translation needed since the vars already matched.
- `Image` (Mantine, used like `next/image` with width/height) in `MITIngestForm.tsx` → real `next/image` per the plan's symbol table (was importing the wrong `Image` from `@mantine/core`, not already on `next/image`).
- `GitHubIngestForm.tsx` had a large dead `createStyles` block (Accordion/logo slots) where only one key (`codeStyledText`) was actually used — deleted the block entirely and inlined that one style as a Tailwind className on the `<code>` element (matching the literal `bg-(--illinois-orange) text-(--illinois-white)` badge style already used in the sibling ingest forms).
- `CanvasIngestForm.tsx`'s `Alert` (icon/color/title/styles.message) → shadcn `Alert`+`AlertTitle`+`AlertDescription`, icon as a plain child SVG (grid-column layout is automatic via `alert.tsx`'s `has-[>svg]` rules). Its `Checkbox` (`label` prop, no shadcn equivalent) → shadcn `Checkbox` wrapped in an implicit `<label>` (checkbox + `<span>` text both inside `<label>`), not an explicit `id`/`htmlFor` pair — Base UI's `Checkbox.Root` renders both a visible `role=checkbox` element and a hidden native `<input>` for form submission, so an explicit `id`/`htmlFor` pointing at one specific id makes `getByLabelText` (and likely some AT) resolve two label-associated controls at once; wrapping avoids the ambiguity. Updated `CanvasIngestForm.test.tsx` accordingly to query `getByRole('checkbox', { name: ... })` instead of `getByLabelText` (role queries correctly exclude the `aria-hidden` native input).
- `WebsiteIngestForm.tsx` and `WebScrape.tsx`'s `SegmentedControl` (vertical, one-at-a-time strategy picker) → hand-rolled `role="radiogroup"`/`role="radio"` button group rather than the shadcn `toggle-group` primitive. Base UI's `ToggleGroup` models pressed state as an array with no built-in "always exactly one selected" constraint, which fights a radio-like use case; a plain controlled button group is simpler, fully accessible by construction, and matches the plan's "toggle-group (single) **or tabs**" either/or.
- `WebScrape.tsx`'s `Tooltip`/`TextInput`/`Text`/`List` around "Max Pages" and "Limit web crawl" converted using the same `IconHelp`-trigger tooltip idiom as `DocGroupsTable.tsx` (the original Mantine `Tooltip` wrapped the whole label+input block as its trigger child, which doesn't map cleanly onto Base UI's single-trigger-element model).
- **Found and fixed the same hardcoded-dark-color bug class as the `DocumentGroupsCard.tsx` incident**: `WebScrape.tsx` had six `<Text style={{ color: '#C1C2C5', ... }}>` instances and one `TextInput styles={{ input: { backgroundColor: '#1A1B1E' } }}` — literal Mantine dark-theme hex values, not CSS vars, so they never tracked the app's light/dark toggle. All converted to `text-(--foreground)` / `bg-(--background-faded)` instead of being carried over literally.
- Deleted dead code encountered along the way: `WebScrape.tsx`'s `useMantineTheme()` call (result assigned to an unused `theme` var) and its `useMediaQuery('(max-width: 960px)')` call (`isSmallScreen`, whose only two use sites were `cond ? 'auto' : 'auto'` — a no-op ternary in the original Mantine code, predating this migration).

**ChatMessage.tsx** (Batch 3, file 1 of 6) — largest file in the migration (2535 lines).

- `createStyles`/`useStyles` (`imageContainerStyle`/`imageStyle`, used for the chat-message image grid) → two plain string constants (`imageContainerClassName`/`imageClassName`) with the equivalent Tailwind classes; both values were already literal (no theme callback), so no vars needed translating.
- `Timer`'s `<Text fz="sm" c="dimmed" mt="sm">` → `<p className="mt-3 text-sm text-(--foreground-faded)">`. Mantine's `c="dimmed"` has no shadcn equivalent name; used the app's existing `--foreground-faded` var (same choice made for every other "muted/secondary text" case this migration).
- Two `Badge` instances (routing-target and tool-output-source pills inside `IntermediateStateAccordion` titles), both `radius="md" size="sm"` with a `styles={{root:{color,backgroundColor}}}` slot pointing at `--foreground`/`--background-dark` (or `--illinois-white`/`--badge-error` for the tool-error state) → shadcn `Badge` with `className="rounded-md bg-(--background-dark) text-(--foreground)"` (conditional on `response.error` for the second one). shadcn's default `Badge` is a full pill (`rounded-4xl`); `rounded-md` is a real, intentional override here (matches Mantine's explicit `radius="md"`), not dropped under the "close enough" bar, since the pill shape is visually distinct from the intended squarer badge.
- One `Tooltip` (wrapping the "Edit message" icon button, `position="bottom"`, inline `style={{color: 'var(--tooltip)', backgroundColor: 'var(--tooltip-background)'}}`) → the standard `Tooltip`/`TooltipTrigger render={<button/>}`/`TooltipContent` idiom, reusing the exact `tooltipContentClassName`/`tooltipArrowClassName` constants already established in the sibling `MessageActions.tsx` (same folder, same `--tooltip`/`--tooltip-background` vars) for consistency rather than re-deriving them.
- **Compound `Modal`** (`FilePreviewModal`, the PDF/text-file preview dialog — `Modal.Root/.Overlay/.Content/.Header/.Title/.CloseButton/.Body`, `size="xl" centered`) → compound `Dialog`/`DialogContent`/`DialogTitle`/`DialogClose`, following the exact pattern already established in `ImagePreview.tsx` (same folder, same `size="xl"` Mantine modal, already converted in Batch 1): `DialogContent` gets `"modal-common w-full max-w-[calc(100%-2rem)] gap-0 p-0 sm:max-w-[788px]"` (the proven `size="xl"` → width mapping from that file) with `showCloseButton={false}` since the header renders its own `DialogClose`+`XIcon` inside `.modal-header-common`, matching the original's custom `Modal.CloseButton`. `Modal.Overlay` needs no separate element — `DialogContent` renders `DialogOverlay` internally. The shared `.modal-*-common` classes are framework-neutral and carried over unchanged, as documented in the plan.
- Dead `Button` import (never referenced in JSX anywhere in the file) — deleted outright.
- No hardcoded literal dark-color bugs (the `style={{color:'white'}}` class of bug from earlier batches) found in this file — all colors already routed through CSS vars.

**PromptEditor.tsx** (Batch 3, file 2 of 6, 2442 lines) — near-duplicate of `PromptEditorEmbed.tsx` (next file); notes below carry over directly.

- `useMediaQuery` (Step 2) confirmed already converted. `useMantineTheme`/`MantineTheme` removed entirely, including the dead `theme` parameter threaded through `showPromptToast`/`showToastOnPromptUpdate` (never actually read inside either function body — a Mantine-era vestige). Removing it changed both functions' call signatures, so the ~17 internal call sites and `PromptEditor.test.tsx`'s calls were updated to drop the now-nonexistent first argument — a genuine, expected consequence of dropping the Mantine type, not a workaround.
- `autosize`/`minRows`/`maxRows` on the system-prompt `Textarea` → restored via the same `useAutosizeTextarea(value)` ref hook already established in `UploadCard.tsx` (same directory) for exactly this purpose, rather than dropping the behavior; `max-h-96 overflow-y-auto` stands in for `maxRows={20}`.
- `Collapse` (Prompt Engineering Guide) → `Collapsible`/`CollapsibleTrigger`/`CollapsibleContent`. The manual `role="button" tabIndex={0} onKeyDown` (Enter/Space) handling on the old trigger was **removed**, not carried over — Base UI's `CollapsibleTrigger` with `nativeButton={false}` already supplies correct `role`/`tabIndex`/keyboard semantics via its own `useButton` hook, including the real spec-accurate distinction that Enter activates on keydown while Space activates on keyup (matching native `<button>` behavior). One test (`toggles guide via keyboard Space key`) encoded the old non-standard "Space activates on keydown" assumption; fixed by firing both `keyDown` and `keyUp` for Space rather than reverting to a less-correct manual handler.
- Two `List`/`List.Item` blocks (Prompt Engineering Guide links, "consider including" bullets) → `<ul>`/`<li>` with a `before:` pseudo-element dot replicating Mantine's custom bullet `icon`. **Correctness fix while converting**: the original nested a Mantine `<Text>` containing a `<List>` inside another `<Text>` containing a `<List>` — since Mantine's `Text` defaults to a non-`<p>` element this was valid, but converting both to `<p>` (the naive per-component mapping) would have produced invalid, auto-closing `<p>`-in-`<p>` (and `<ul>`-in-`<p>`) nesting. Used `<div>` for both wrapping elements instead, since each wraps block content.
- The System Prompt model `Select` (searchable, grouped by provider via the existing `group` field on `modelOptions`) → the `combobox.tsx` wrapper, mirroring `ModelSelect.tsx`'s exact grouping pattern (`ComboboxGroup`/`ComboboxCollection`), via a small `groupedModelOptions` derivation (adjacent-run grouping, since `modelOptions` is already sorted by `LLM_PROVIDER_ORDER` with same-provider items contiguous). `styles={(theme) => ({...})}` (root/input/dropdown/item/rightSection, ~40 lines) → dropped entirely in favor of the wrapper's defaults plus a handful of Tailwind classes for the width/color pieces that didn't already match.
- The Tooltip on that Select (`styles={{tooltip:{backgroundColor: theme.colors.dark[7], color: theme.white}}}`) — **dropped**, confirmed the default shadcn `Tooltip` (`bg-foreground text-background`) already renders a dark-bg/light-text tooltip in this app's theme, matching the intent.
- Flat `Modal` (Optimization results dialog, `size="xl" centered radius="lg"`) → `Dialog`/`DialogContent`/`DialogTitle`/`DialogClose`, same `sm:max-w-[788px]` width mapping used for `size="xl"` elsewhere this batch. Its two footer buttons (`Cancel` outline + `Update System Prompt`) had `styles`/`sx` overrides that already pointed at `--foreground`/`--background-faded`/`--dashboard-button*` — dropped for `Cancel` (default `outline` variant's `hover:bg-accent` was a real mismatch, a distinct blue vs. the wanted neutral faded gray, so kept a small className override) and dropped entirely for the second button by switching to `variant="dashboard"`.
- **Two duplicate "Reset Prompting Settings" confirmation `Modal`s** (one in the `isEmbedded` branch, one in the non-embedded sidebar branch — pre-existing duplication in the original Mantine code, not introduced or removed here; left duplicated rather than extracting a shared component, since deduplicating is a separate refactor beyond this migration's scope). Both are **intentionally, permanently dark-themed** regardless of the app's light/dark toggle (hardcoded `#15162c`/`#2D2F48`/`white`/`#D1D1D1`, self-consistent bg+text pairing) — unlike the recurring bug class from earlier batches (hardcoded light text on a surface that _does_ track the theme), so these were kept as intentional dark-mode-only UI, not "fixed" to use `--modal`/`--foreground`. `#15162c` maps onto the existing `--illinois-purple-dark` CSS var (an exact match already in `globals.css`) rather than being re-hardcoded; `#2D2F48` has no existing var and matches the same literal already used for a modal border in `LinkGeneratorModal.tsx` (Batch 1 precedent), so kept literal for consistency. The gradient `Text variant="gradient" gradient={{from:'red',to:'white',deg:45}}` title → `bg-clip-text text-transparent` with an inline `backgroundImage: linear-gradient(45deg, red, white)` (no Tailwind utility covers an arbitrary two-stop gradient at 45deg cleanly). Gray/red button colors (`theme.colors.gray[6]/[8]`, `theme.colors.red[8]/[9]`) → Tailwind's `gray-600`/`gray-800`, `red-800`/`red-900` (closest standard-palette match, consistent with the dark-modal aesthetic — deliberately not `variant="danger"`'s default `red-600`/`red-700`, which reads too bright/light-mode-ish here).
- Four `Button`s outside the modals with `leftIcon` (no shadcn equivalent) → icon rendered as a plain child before the button text, with `gap-2` on the button.
- Two `Indicator` "New" badges (positioned near section headings, different `top`/`right` offsets each) → `<span className="relative inline-block">` wrapping an absolutely-positioned shadcn `Badge`, colors carried over via `bg-(--dashboard-button)`/`text-(--dashboard-button-foreground)` (Badge's own default `bg-primary` didn't match).
- No other hardcoded-color bugs found beyond the two already-decided-intentional dark modals above.

Verification: `grep @mantine` → 0 hits. Typecheck clean. `PromptEditor.test.tsx` — 63/63 pass (2 fixes: dropped `theme` args per the signature change above; Space-key test fixed to fire `keyUp`). Full smoke suite (6 shards) green. ESLint: only pre-existing `any`/unused-var warnings, 0 errors.

Verification: `grep @mantine` → 0 hits. Typecheck clean. All 6 directly-related test files pass (138 tests: `ChatMessage.test.tsx`, `ChatMessage.additional.test.tsx`, `ChatMessage.a11y.test.tsx`, `ChatMessage.markdownCoverage.test.tsx`, `MessageActions.test.tsx`, `MessageActions.a11y.test.tsx`) with zero test changes needed — the DOM shape stayed compatible with existing assertions (e.g. `getByLabelText('Close file preview')` still resolves via the preserved `aria-label` on `DialogClose`). Full smoke suite (6 shards) also green.

**PromptEditorEmbed.tsx** (Batch 3, file 3 of 6, 2160 lines) — `PromptEditor.tsx`'s near-duplicate; reused that file's already-verified conversions wherever the code was structurally identical, adjusted for this file's own actual features (it has no `disableCitations` toggle/prompt-splice logic, and keeps its own original fallback model — `'gpt-4'`/`'GPT-4'`/`tokenLimit: 8192` — rather than picking up `PromptEditor.tsx`'s newer, unrelated default; that's a pre-existing content difference between the two files, not something to sync).

- Same `useMantineTheme`/`MantineTheme`/dead-`theme`-parameter cleanup as `PromptEditor.tsx` — `showPromptToast`/`showToastOnPromptUpdate` lost their unused first `theme` argument; updated all internal call sites plus 8 call sites in `PromptEditorEmbed.test.tsx` that passed `{} as any` for it.
- `autosize`/`minRows`/`maxRows` on the Textarea → the same `useAutosizeTextarea` hook from `UploadCard.tsx`, matching `PromptEditor.tsx`.
- `Collapse` → `Collapsible`/`CollapsibleTrigger`/`CollapsibleContent`, `List`/`List.Item` → `<ul>`/`<li>` with the `before:` bullet-dot pattern — identical slots to `PromptEditor.tsx`, resolved the same way.
- **Real functional difference preserved, not dropped**: unlike `PromptEditor.tsx`'s already-converted combobox (which has no per-model country-of-concern warning), this file's _original_ Select genuinely had one — both a per-item warning inside the dropdown (`itemComponent`) and a warning next to the trigger for the currently-selected model (`rightSection`). Converting off `PromptEditor.tsx`'s combobox structure without checking would have silently dropped this a11y/safety feature. Restored both using the exact pattern already established in `ModelSelect.tsx`'s `ModelItem`/trigger (`Tooltip`/`TooltipTrigger render={<span/>}`/`TooltipContent`, `IconAlertTriangle` at `text-yellow-500` — matching `ModelSelect.tsx`'s color choice over the original's literal `#f59e0b`, since it's the same warning icon used everywhere else in the app).
- Flat `Modal` (optimization results) and the two duplicated "Reset Prompting Settings" modals → identical `Dialog` conversion to `PromptEditor.tsx` (same `--illinois-purple-dark`/`#2D2F48` choices for the intentionally-permanently-dark reset modals, same gradient-title technique, same button color mapping).
- Two `Indicator` "New" badges → same `Badge` + absolute positioning technique as `PromptEditor.tsx`.
- No new hardcoded-color bugs found (all colors already routed through vars, aside from the two modals already established as intentionally dark).

Verification: `grep @mantine` → 0 hits. Typecheck clean. `PromptEditorEmbed.test.tsx` — 54/54 pass (8 call-site fixes for the dropped `theme` parameter, same as `PromptEditor.test.tsx`). Full smoke suite (6 shards) green.

**ChatInput.tsx** (Batch 3, file 4 of 6, 1536 lines — the primary chat message input, no behavior changes needed):

- `Text` (single usage, the "Chat Settings"/model-picker pill) → plain `<span>`. Mantine's `size={isSmallScreen ? '10px' : 'xs'}` → `${isSmallScreen ? 'text-[10px]' : 'text-xs'}` in the className (matches the existing conditional-size idiom used elsewhere in this file).
- `Tooltip` (single usage, the country-of-concern warning next to the model name) → `Tooltip`/`TooltipTrigger render={<span/>}`/`TooltipContent className="max-w-[280px] text-wrap"`, the exact idiom already established in `ModelSelect.tsx`. The icon's hardcoded `style={{ color: '#f59e0b' }}` → `className="text-yellow-500"` for consistency with `ModelSelect.tsx`'s identical warning icon (same semantic color, now expressed the same way everywhere in the app instead of two different literal spellings of amber).
- `useMantineTheme` → deleted; the resulting `theme` binding was **dead code** (`theme.` had zero references anywhere in the file) — confirmed via full-file grep before removing, not just assumed.
- **Hardcoded-color sweep (3 known sites + a full-file check for others):**
  - Two `color: 'white'` sites (small checkmark/x SVG icons centered inside fixed-color status circles — `var(--illinois-prairie)` green for "completed", `var(--destructive)` red for "error") — **not the bug**. These are theme-independent status badges: the circle's own background is always a fixed semantic color (success green / error red) regardless of the app's light/dark toggle, so white icon-on-colored-circle is correct and intentional in both modes. Left unchanged.
  - `style={{ color: '#f59e0b' }}` (the country-of-concern warning triangle) — also not the ambient-color bug (it's a fixed warning-accent color, not text sitting on a `--modal`/`--background` surface), but converted to `text-yellow-500` anyway for consistency with the identical icon in `ModelSelect.tsx`/`PromptEditorEmbed.tsx`, which already established that Tailwind-class spelling for this exact warning.
  - No other `style={{ color: ... }}`/`backgroundColor` literals found in a full-file grep beyond the 3 known sites — no new instances of the recurring bug class in this file.
- No Mantine-specific behavior quirks found with no Base UI equivalent — the sole `Tooltip` conversion is a drop-in replacement (Base UI's `TooltipRoot` needs no provider, as established in every prior batch), and no other interaction (keyboard send/newline, paste, drag-drop, focus/disabled states) touches Mantine at all — those were already plain React/DOM code, untouched by this conversion.

Verification: `grep @mantine` → 0 hits. Typecheck clean. `ChatInput.test.tsx` (23 tests) + `ChatInput.a11y.test.tsx` (2 tests) — 25/25 pass, **zero test changes needed**. Full smoke suite (6 shards) green.

**Chat.tsx** (Batch 3, file 5 of 6, 2204 lines — the main chat page orchestrator; minimal remaining Mantine surface, no style-slot translations needed):

- `Text` (single usage — the course's `course_intro_message`, rendered via `dangerouslySetInnerHTML` in the welcome card) → plain `<p>`. Added an explicit `text-(--welcome-foreground)` class that the original `<Text>` never had: Mantine's `Text` always computes an explicit color itself (this session's now-familiar landmine — `colorScheme: 'dark'` is still hardcoded in `_app.tsx`), so this element's color was never actually ambient. Its sibling `<h4>` two lines down already explicitly uses `text-(--welcome-foreground)` on the same `bg-(--welcome-background)` card — matched that instead of leaving the new `<p>` to inherit from `body`, which happens to resolve correctly today only because of the earlier `body` color fix elsewhere in this migration. Being explicit here removes the dependency on that coincidence, consistent with how every other themed surface in this codebase pairs its own foreground/background vars.
- `Button` (single usage — a decorative, non-interactive wrapper (`tabIndex={-1}`) around an example-question row; the actual click/keydown handling lives on the parent `<div role="button">`) → shadcn `button.tsx`, `variant="link"` (already exists in the shared component, no changes needed there). Added `type="button"` — matching Mantine's own actual default (`type="button"`), whereas a bare native `<button>` defaults to `type="submit"`; this component doesn't currently sit inside a `<form>`, but the addition costs nothing and is a faithful port of Mantine's real behavior, not a new default.
- Checked specifically for the failure modes found in ChatMessage/PromptEditor/PromptEditorEmbed's conversions this session:
  - **No fragile absolute-positioned "badge near text" hack.** No `Indicator` usage in this file at all. The one absolutely-positioned UI element (the "Admin Dashboard" gear icon + its hover tooltip, `top-4 right-4` / `top-full right-0`) is a plain CSS `group`/`group-hover` tooltip positioned relative to its own real, correctly-sized parent box — not an empty zero-size anchor span with magic pixel offsets. Confirmed absent, not just unfixed.
  - **No textarea/input autosize race.** No textarea or size-measuring effect in this file — `scrollHeight`/`scrollTop` usages here are all legitimate imperative chat-scroll-position logic (auto-scroll-to-bottom, preserve scroll offset across streamed message updates), not a React `style`-prop-vs-imperative-write conflict; none of those elements take a JSX `style` prop that could be wiped. Confirmed absent.
  - **No `items-center` on a `flex-col` wrapper shrink-centering a child that should stretch.** Grepped for the pattern; the file has no `flex-col`+`items-center` combination at all. Confirmed absent.
  - **No React `style` prop coexisting with an imperative `el.style.x =` write.** Confirmed absent — no refs in this file mutate `.style` directly.

Verification: `grep @mantine` → 0 hits. Typecheck clean. All 4 directly-related test files — `Chat.test.tsx`, `Chat.additional.test.tsx`, `Chat.queryRewrite.test.tsx`, `Chat.coverage.test.tsx` — 114/114 pass, **zero test changes needed**. Full smoke suite (6 shards) green.

**LLMsApiKeyInputForm.tsx** (Batch 3, file 6 of 6 — the last file of Batch 3 — 1273 lines, `ActionIcon`/`Button`/`Card`/`Flex`/`Group`/`Select`/`Stack`/`Text`/`TextInput`/`Title`/`Tooltip`):

- `ActionIcon` (the "Clear" button on `APIKeyInput`) → `<Button variant="ghost" size="icon-xs">`.
- `Button` → shadcn `button.tsx`; the `compact` "Save" button → `size="xs"`.
- `Card` (root of the non-embedded layout) → shadcn `card.tsx`. `withBorder padding="none" radius="xl"` → the exact `rounded-4xl border py-0` className string already established for this shape in `DocumentGroupsCard.tsx`/`DocumentsCard.tsx`/`ApiKeyManagament.tsx` (reused verbatim rather than re-derived, for consistency across the four cards that share it).
- `Flex`/`Group`/`Stack` → Tailwind flex utilities throughout. The one responsive `direction={{ base: 'column', '75rem': 'row' }}` (75rem = 1200px, no matching Tailwind breakpoint — this repo doesn't add custom `screens`, per the plan's global rules) → approximated with `xl:` (1280px), the closest built-in breakpoint; a layout-only column/row switch, not a pixel-critical value, so "close enough" applies.
- `TextInput` → shadcn `input.tsx` wrapped in an explicit `<label htmlFor>` (shadcn's `Input` has no built-in `label` prop, unlike Mantine's `TextInput`); kept the original `aria-label` too since it takes precedence in the accessible-name computation, so behavior (and existing `getByLabelText` test queries) didn't change.
- `Text`/`Title` → native elements + `.heading-h1`–`.heading-h6` classes.
- **The 5th and last searchable `Select` in the whole migration** (`NewModelDropdown`'s default-model picker) → the `combobox.tsx` wrapper, grouped by provider via `ComboboxGroup`/`ComboboxCollection`/`ComboboxCollection`, mirroring `ModelSelect.tsx`'s exact integration (grouping shape, `isItemEqualToValue`, provider-logo `Image` in the input group, country-of-concern `Tooltip`+`IconAlertTriangleFilled` next to the trigger). Kept the accessible name as a plain `aria-label="Select a model"` directly on `ComboboxInput` (matching the original `placeholder`) rather than the original's separate `aria-labelledby`-driven "Select default model" sr-only label, since `aria-labelledby` would have taken precedence in the accname algorithm and silently renamed the control.
- The file's own `ModelItem` (used as a plain child of `ComboboxItem`, not as a Mantine `itemComponent` render-prop target anymore) lost its `forwardRef` + `{...others}` prop-spreading entirely — that machinery existed only to satisfy Mantine's old itemComponent contract (ref + arbitrary DOM props forwarded onto the item root), which no longer applies now that Base UI's `ComboboxItem` is itself the interactive/focusable element. Also dropped its unused `loadingModelId`/`isDownloaded` props (dead — the body never read them, and the commented-out check they existed for was already commented out before this conversion).
- Checked specifically for the failure modes found in earlier Batch 3 conversions (fragile absolute-badge hacks, textarea autosize races, `items-center` shrink-centering a should-stretch child, JSX `style` vs. imperative `.style` writes) — **none present**. The one `flex-col`+`items-center` in this file (wrapping the `Card`) is not the bug: the Card has an explicit width (`useResponsiveCardWidth`'s `w-[…%]` classes, always < 100%), so centering it is the correct, intentional behavior — matching the original Mantine `<Flex direction="column" align="center">` — not a child relying on an ancestor's `w-full` to imply its own width the way `PromptEditor.tsx`'s bug did.
- Test fixes (all 3 test files touching this component): removed a `vi.mock('@mantine/core', …)` block in two files that faked the Select as a native `<select>` — now dead since the component no longer imports `@mantine/core` at all. Converted every `user.selectOptions(...)`/`within(select).getAllByRole('option')` call (native-`<select>`-only APIs) to open-the-combobox-then-click-the-option / read-`getAllByRole('option')`-after-opening, via two small shared helpers per file. Removed one test (`ModelItem > forwards ref and extra props`) that asserted the now-retired Mantine-`itemComponent` ref/prop-forwarding contract described above — there is no replacement behavior to assert since that contract doesn't exist anymore, not a coverage gap.

Verification: `grep @mantine` → 0 hits. Typecheck clean. All 3 directly-related test files — `LLMsApiKeyInputForm.test.tsx` (UIUC-Components, 3 tests), `LLMsApiKeyInputForm.additional.test.tsx` (38 tests), `api-inputs/__tests__/LLMsApiKeyInputForm.test.tsx` (38 tests) — 79/79 pass. Full smoke suite (6 shards) green, plus `PromptEditor.test.tsx`/`PromptEditorEmbed.test.tsx` (117 tests, both import `findDefaultModel` from this file) re-verified green. Full project suite: 289/289 files, 2460/2460 tests (one fewer than before — the removed obsolete test). Production build clean.

**Batch 3 complete** — all 6 of the plan's largest files (ChatMessage, PromptEditor, PromptEditorEmbed, ChatInput, Chat, LLMsApiKeyInputForm) are now converted off Mantine.

**LargeDropzone.tsx** (Step 4c, 403 lines, `@mantine/dropzone`'s `Dropzone` + `@mantine/core`'s `Group`/`Text`/`rem` — hand-rolled drag handlers, no `react-dropzone` dependency added):

- The dropzone itself is now a plain `<div role="button" tabIndex={0}>` with native `onDragEnter`/`onDragOver`/`onDragLeave`/`onDrop` handlers, plus a hidden `<input type="file" multiple>` for click-to-browse (clicking the div calls `fileInputRef.current?.click()`; Enter/Space on the div do the same via `onKeyDown`, since a `<div role="button">` gets no free keyboard activation the way a real `<button>` would).
- **`Dropzone.Reject` was confirmed dead** (grepped — the original `<Dropzone>` receives no `accept` prop anywhere, so Mantine could never enter a rejected-drag-state) and deleted entirely, including its two render branches (icon + message) and the `IconX`/`var(--error)` styling that went with it. No `isRejected` state was built. A single `isDragging` boolean now covers what `Dropzone.Accept`/`Dropzone.Idle` used to.
- **Dragleave-on-child-element fix**: real `dragenter`/`dragleave` DOM events fire on every element boundary the cursor crosses, including children inside the dropzone (the icon, the hint text, `SupportedFileUploadTypes`) — a bare `onDragLeave → setIsDragging(false)` would flicker the visual state constantly while dragging across those children. Used a counter (`dragCounterRef`, incremented on enter, decremented on leave, `isDragging` only clears when it nets back to `0`) rather than a `relatedTarget`/`contains` check, since the counter approach doesn't need to reason about the DOM tree shape at all. Verified this actually holds with a real DOM event simulation: enter the outer dropzone, enter a child, leave the child (dropzone must still read as "dragging"), then leave the dropzone itself (must clear) — see the new `LargeDropzone.test.tsx` test `stays in the dragging state while the cursor crosses a child element`.
- `openRef` was confirmed never called anywhere (grepped) and dropped, per the plan.
- `loading={uploadInProgress}` → an `aria-busy` attribute on the dropzone (always present, toggles `true`/`false`) plus a conditionally-mounted absolute overlay (`data-testid="dropzone-loading-overlay"`) showing the existing shared `LoadingSpinner` component, replacing Mantine's built-in `LoadingOverlay`. Also threaded `uploadInProgress` into an `interactionDisabled = isDisabled || uploadInProgress` flag so drag/drop/click are inert while an upload is in flight (matching Mantine's own `loading` prop, which disables the dropzone) — the original code never explicitly modeled this, but Mantine did it implicitly and no test relied on being able to drop a second batch mid-upload.
- `Group position="center" pt={rem(12)}` → `flex items-center justify-center gap-2 pt-3` (`rem(12)` = 0.75rem = Tailwind's `pt-3`, exact). `rem(200)`/`rem(12)` (min-height/border-radius) → `12.5rem`/`0.75rem` literal values (rem() is just px/16, both convert exactly with a 16px root). Icon `size={rem(30/50)}` → plain numeric `30`/`50` (tabler icons' `size` prop takes a bare pixel number identically to a `"1.875rem"` string at the default root font size — simpler, so kept the number).
- `Text` → native `<p>`, with `fz`/`mt` mapped onto the existing Tailwind scale: `fz="md"`→`text-base`, `fz="lg"`→`text-lg`, `fz="xs"`→`text-xs`, `fz="sm"`→`text-sm` (Mantine's md/lg/xs/sm point sizes already line up with Tailwind's same-named steps); `mt="md"`→`mt-4`, `mt="xl"`→`mt-8` (Mantine's spacing scale, per the plan's global-rules table, plus its default `xl`=32px); `mt="xs"`→`mt-2.5` (Mantine `xs`=10px, and Tailwind has an exact `2.5` step = 0.625rem = 10px).
- Checked the four recurring failure modes from earlier batches: no absolute-positioned badge/indicator anywhere in this file (confirmed absent); the one DOM-size-adjacent concern — the loading overlay's mount/unmount — uses plain conditional rendering, not a ref-measured effect, so the value-only-effect race doesn't apply; no `flex-col`+`items-center` wrapping a child lacking its own width (the outer two wrapper divs use inline `style` objects with `flex`/`alignItems`/`justifyContent`, not Tailwind's `items-center` class, and both wrap a dropzone that has its own `width: '100%'` — not the bug shape); no element combines a JSX `style` prop with an imperative `.style` write (the component sets no styles imperatively at all now).
- Test file rewrite: removed the `vi.mock('@mantine/dropzone', …)` block (which faked `Dropzone` as a `<button>` labeled "trigger-drop" plus a `capturedOnDrop` escape hatch) and the dead `vi.mock('@mantine/hooks', …)` (the component already imported `useMediaQuery` from the shadcn hook, not `@mantine/hooks` — that mock had no effect even before this conversion). Added two helpers, `dropFiles(files)` (real `fireEvent.drop` on `screen.getByTestId('dropzone')`) and `dropDefaultFile()` (single-PDF convenience matching the old mocked button's canned drop), and replaced every `capturedOnDrop?.([...])` / `user.click(trigger-drop button)` call site with one of them. The loading-state test now reads `aria-busy` + the overlay's presence instead of the mock's `data-testid="dropzone-loading"` text node. Added 3 new tests for real drag-state behavior (enter/leave visual toggle, the child-crossing edge case, and disabled-state drag suppression) that the old mock-based test couldn't exercise at all, since it never simulated real drag events.

Verification: `grep @mantine` → 0 hits. Typecheck clean (one unrelated pre-existing error surfaced in `MakeQueryAnalysisPage.tsx` from a different in-flight Step 4 task, not touched here). `LargeDropzone.test.tsx` — 39/39 pass (36 original + 3 new). `UploadCard.test.tsx` (renders `LargeDropzone` as a child) — 14/14 pass. Full smoke suite (6 shards) green.

**ProjectFilesTable.tsx** (Step 4a, the hardest single piece of the migration — 1523 lines, `mantine-datatable`'s `DataTable` rendered twice + `@mantine/core`'s `ActionIcon`/`Box`/`Button`/`Code`/`CopyButton`/`Group`/`Image`/`Indicator`/`Modal`/`MultiSelect`/`Paper`/`Stack`/`Text`/`TextInput`/`Tooltip`/`createStyles`), plus two new supporting files it required:

- **`DataTable` → plain shadcn `<Table>`** (both renders — the main table and the error-state table). Sorting/pagination stayed exactly where they already lived (React state feeding the existing `sortStatus`/`page` query-key params); no `@tanstack/react-table` introduced, per the plan. Built a small in-file `SortableColumnHeader` (button + chevron/`IconArrowsSort`, toggles `asc`/`desc` on repeat clicks) and a `FilterInput` (a second header row, one always-visible input per filterable column writing into the existing single `filterKey`/`filterValue` state pair) to replace Mantine's per-column filter-popover UI — simpler and more testable than reproducing a popover, since only one filter can ever be active at a time regardless of UI shape.
- **The dead `useEffect` that DOM-patched `button.mantine-ActionIcon-root[aria-haspopup="dialog"]`** (labeling Mantine's own generic-role filter-popover buttons for axe) was deleted outright rather than translated — there's no popover left to patch. The new markup is correct by construction instead: every icon button (`View document`, `Delete document`, refresh, copy, chip-remove) carries a real `aria-label`, decorative icons carry `aria-hidden`, and the row/select-all checkboxes use `Checkbox`'s own `aria-label`. `npm run test:a11y` (60 tests across all 15 `*a11y*` files) stays green — this file still isn't covered by any of them (unchanged from before), so this is "correct by construction + no regression," not new coverage; a real browser axe pass would still be the stronger signal per the plan, but wasn't run in this sandboxed session.
- **New `src/components/UIUC-Components/TablePaginationFooter.tsx`** — the shared pagination footer the plan calls for. Props: `{ page, totalRecords, recordsPerPage, onPageChange, className? }`. Renders a "`from`–`to` of `total`" label plus prev/next icon buttons (disabled at the bounds) and a "`page` / `totalPages`" indicator — no records-per-page selector, matching that neither table exposes one today. **`N8nWorkflowsTable.tsx`'s own conversion task should import and reuse this component as-is** rather than building a second one.
- **New `src/components/UIUC-Components/DocGroupMultiSelect.tsx`** — the Step 4b `MultiSelect`→combobox replacement, built on the existing `combobox.tsx` wrapper. Props: `{ data: {value,label}[], value: string[], onChange: (value: string[]) => void, placeholder?, nothingFoundText?, disabled?, className?, 'aria-label'? }` — deliberately the same shape as the old Mantine `MultiSelect` usage (`data`/`value`/`onChange` of plain strings) so both call sites in `ProjectFilesTable.tsx` (the bulk-assign dropdown and the in-cell editor) dropped in with no data-shape translation. **Creatable is hand-built** (~65 lines, no Dialog-confirmation step — unlike the Base UI docs' own creatable recipe, this matches the original Mantine `creatable` UX of immediate create-on-select): a sentinel item (`{ creatable: trimmed, value: 'create:'+lowered, label: '+ Create "..."' }`) is appended to the filtered list whenever the query has no exact label match; `onValueChange` detects the sentinel in the new selection, strips it, and pushes a plain `{value: name, label: name}` in its place before calling `onChange` with plain strings — the sentinel object never leaks out. `autoHighlight` is set so plain "type and press Enter" creates/selects immediately, matching Mantine's old feel, without a hand-rolled keydown handler. Item shape is `{value, label}` (not `{id, label}`) specifically because Base UI's combobox auto-derives both filtering and display text from that exact shape (documented in `combobox.tsx`'s own comments) — using `id` instead would've required explicitly wiring `itemToStringLabel`/`itemToStringValue` for no benefit. New test file `DocGroupMultiSelect.test.tsx` (4 tests: renders selected chips, filters+selects an existing option, creates-and-selects a brand-new name with the plain string round-tripping out, removes a chip via its remove button) — all pass.
- **`CopyButton`** (Error Details modal) → `navigator.clipboard.writeText` + local `isCopied` state + `setTimeout` reset, the exact pattern from `CodeBlock.tsx:65`, wrapped in the standard `Tooltip`/`TooltipTrigger`/`TooltipContent` idiom (tooltip text flips "Copy"/"Copied" the same as before).
- **`Modal` → `Dialog`** for all three modals (delete-confirm, error-details, export-confirm) — all three were the flat `<Modal opened onClose title>` shape, converted to `Dialog`/`DialogContent`/`DialogHeader`/`DialogTitle`/`DialogFooter` per the established `FeedbackModal.tsx`/`LinkGeneratorModal.tsx` idiom.
- **Found and fixed a live instance of the recurring hardcoded-white-text bug** while converting the export-confirm modal: `<Text size="sm" style={{ color: 'white' }}>` sat directly on the `--modal` surface (light in this app's actual theme) — same bug class fixed 6+ times earlier this session elsewhere. Changed to `text-(--modal-text)`.
- **`Image` (error-state's "this-is-fine.jpg")** → plain `<img>`, not `next/image`: `assets.kastan.ai` isn't in `next.config.mjs`'s `remotePatterns` allowlist, so this follows the `CitationCard.tsx` precedent from Batch 2 (arbitrary external host, flagged rather than silently forced through `next/image`).
- **The "Failed" tab's count badge** (`Indicator` wrapping the tab button) → a `Badge` positioned `absolute -top-1.5 -right-1.5` inside a `<div className="relative inline-flex">` that wraps the _real, normally-sized_ "Failed" button itself (not an empty anchor span) — a proper corner-notification-dot layout, not the fragile empty-anchor-plus-magic-offset pattern that broke `PromptEditor.tsx`'s "New" badges earlier in this session. Checked this deliberately given that history.
- **Checked all four recurring failure modes from earlier Batch 3/Step 4 fixes**: (1) fragile absolute-badge-vs-empty-anchor — avoided as above; (2) textarea/DOM-size measured via a value-only effect — not applicable, no such element in this file (the error-overflow-detection effect correctly depends on `[failedDocuments, tabValue]`, i.e. the async data itself, not a value disconnected from when the element mounts, so no two-tick race exists here); (3) `flex-col`+`items-center` shrink-wrapping a should-stretch child — none introduced (checked every `flex-col` in the file); (4) JSX `style` prop coexisting with an imperative `.style` write — none (checked every `style={{...}}` in the file).
- A cross-file integration note for whoever reconciles the parallel Step 4 tasks: `showToastOnUpdate` (exported from `MakeQueryAnalysisPage.tsx`, being converted in a separate parallel task) still has a dead, unused `theme: MantineTheme` leading parameter in its _current_ on-disk state. Since importing the `MantineTheme` type here would reintroduce a `@mantine/core` import, the call site now passes `undefined as any` as that first argument with a comment explaining why. **If/when `MakeQueryAnalysisPage.tsx`'s conversion drops that dead parameter (likely, given the same cleanup happened for `PromptEditor.tsx`'s `showPromptToast`/`showToastOnPromptUpdate`), this call site will need its extra argument removed** — a one-line follow-up, not a design problem.

Verification: `grep @mantine\|mantine-datatable` → 0 hits across `ProjectFilesTable.tsx`, `DocGroupMultiSelect.tsx`, `TablePaginationFooter.tsx`. Typecheck clean project-wide except for `MakeQueryAnalysisPage.tsx`'s in-progress parallel edit (confirmed by re-running after a delay: zero errors outside that one file, whose errors are transient mid-save JSX syntax, not something introduced by this task). `ProjectFilesTable.test.tsx` — fully rewritten against the real markup (the old file entirely mocked away `mantine-datatable` and `@mantine/core`'s `MultiSelect`, which no longer exist to mock) — 5/5 tests pass, covering the same scenarios as before (filter/sort/view/select/delete/export) plus the row- and bulk-level document-group-assignment flows exercised through the real `DocGroupMultiSelect` combobox instead of a stubbed one. `npm run test:a11y` — 15/15 files, 60/60 tests pass. Full 6-shard smoke suite green.

**MakeQueryAnalysisPage.tsx** (Step 4c's date-picker piece, combined with this file's full remaining Mantine cleanup — 1306 lines, `@mantine/core`'s `Button`/`Flex`/`Select`/`Text`/`Title`/`createStyles`/`MantineTheme` plus `@mantine/dates`'s `DatePickerInput`; this file was never assigned to an earlier Step 3 batch, so it carried its complete original Mantine surface until now):

- **`DatePickerInput` (`type="range"`) → `Popover` + `Calendar` (`mode="range"`)**. A `Button variant="outline"` trigger (rendered via `PopoverTrigger`'s `render` prop, matching the `TooltipTrigger`/`DialogTrigger` idiom already established elsewhere) shows a formatted `"Jan 1 – Jan 5"`-style label (`formatDateRangeLabel`, a small module-level helper) or "Pick date range" as a placeholder; the popover contains a single `<Calendar mode="range" selected={{from, to}} onSelect={...}>`, converting react-day-picker's `DateRange` shape to/from the existing `[Date|null, Date|null]` tuple state so no other code in the file (the `getDateRange()` switch, the fetch effect) needed to change.
- **Two plain `Select` instances** (date-range filter: all/last_week/last_month/last_year/custom; view: hour/weekday) → shadcn `select.tsx`, using `SelectTrigger`'s built-in `size="sm"` instead of hand-rolling Mantine's `size="sm"`/`size="xs"` height. **Caught the same `SelectValue`-shows-raw-value-not-label bug found in `FeedbackModal.tsx` during Batch 1**: `<SelectValue />` with no children function displays the underlying `value` string (e.g. `"weekday"`) rather than the option's `label` (`"By Day of Week"`) — added the same `{(value) => OPTIONS.find(...)?.label ?? placeholder}` children-render-prop fix to both instances. Caught this via the test suite, not by inspection — worth flagging since it's an easy one to miss silently (the UI still "looks right" at the moment of selection in a manual click-through, since focus/hover states mask it, but breaks on any deferred re-render).
- `Button` → `button.tsx` with `variant="dashboard"`; Mantine's `rightIcon` prop (no shadcn equivalent) became a trailing child instead. The button's dedicated `createStyles` object (`downloadButton`: custom gap/radius/height/three responsive-width media queries) was dropped entirely rather than translated — it was fighting with the _already-present_ Tailwind responsive classes on the same element (`w-full ... sm:w-auto sm:px-4 sm:text-base`), a redundant second responsive system; kept only the Tailwind side plus `h-12 rounded-2xl gap-2.5` (exact equivalents of the dropped `height: 48px` / `theme.radius.xl` / `theme.spacing.xs`).
- `Flex direction="column" align="center" w="100%"` (the page's outermost content wrapper) → `flex w-full flex-col items-center`. This is one of the few `items-center` cases that's genuinely correct, not the recurring shrink-wrap bug: its one child already carries its own explicit width (`w-[96%] md:w-full 2xl:w-[95%]`), so centering it is the actual intent, not an accident — checked deliberately given this failure mode's history this session.
- `Text`/`Title` (~35 call sites: stat cards, trend badges, section headers) → native `<p>`/`<h4>` + Tailwind (`size="sm"`→`text-sm`, `weight={700}`→`font-bold`, `mb={1}`→`mb-px`, `mb="xl"`→`mb-8`, etc. — Mantine's default font-size scale and this app's spacing-token overrides both already line up with Tailwind's matching-named/pixel steps, so no value needed hand-tuning). All remaining `Title`s were `order={4}` → `.heading-h4`.
- Deleted `showToastOnFileDeleted` — a fully dead, unexported, never-called function in this file (confirmed via full-file grep) that only existed to satisfy the same now-removed `MantineTheme` import.
- **Cross-file consideration**: `showToastOnUpdate` is exported from this file and imported by `ProjectFilesTable.tsx` (converted in a parallel Step 4 task). Its `theme` parameter was already fully unused in the function body, but rather than removing it (which would've required coordinating an arg-count change with that concurrently-edited call site), retyped it from `MantineTheme` to `unknown` and kept the same 4-parameter arity — any caller, converted or not, remains valid without either file needing to know what the other did.
- Checked all four recurring failure modes: (1) no absolute-positioned badge/indicator anywhere in this file; (2) the four `useEffect`-driven data fetches all set their loading flags in the _same_ effect that sets the data, so no two-tick "value changes before element mounts" race exists (the file has no ref-measured DOM sizing at all); (3) the one `items-center` usage is the genuine-centering case described above, not the shrink-wrap bug; (4) no element combines a JSX `style` prop with an imperative `.style` write.

Verification: `grep @mantine` → 0 hits. Typecheck clean (project-wide, excluding a pre-existing error in the concurrently-edited `ProjectFilesTable.tsx`). `MakeQueryAnalysisPage.test.tsx` — fully updated (removed both `vi.mock('@mantine/core', …)` and `vi.mock('@mantine/dates', …)`, converted every `user.selectOptions` call to open-then-click-option against the real Base UI Select, and the custom-range flow to open-popover-then-click-two-calendar-day-buttons) — 7/7 pass. One test-infra note: Base UI's Select popup stays mounted (`data-closed`, not removed) through its close transition, which never fires a completion signal in jsdom — reopening the same Select in rapid succession within one test trips `userEvent`'s pointer-events visibility guard against the stale popup; worked around with `userEvent.setup({ pointerEventsCheck: 0 })` for that one multi-reselection test, real pointer simulation still used everywhere else. Full 6-shard smoke suite green.

**N8nWorkflowsTable.tsx** (Step 4a's second table, completing the `ProjectFilesTable`/`N8nWorkflowsTable` build-out in full — 323 lines, `@mantine/core`'s `Switch`/`Text` plus `mantine-datatable`'s `DataTable`). Much simpler than `ProjectFilesTable.tsx`: client-side records (all fetched at once via `useFetchAllWorkflows`), a fixed `createdAt desc` sort with no user-facing sort control, no row selection.

- **`DataTable` → plain shadcn `<Table>`**, reusing `ProjectFilesTable.tsx`'s brand-new `TablePaginationFooter` component as-is (`{page, totalRecords, recordsPerPage: PAGE_SIZE, onPageChange}`) rather than building a second footer — exactly as that task's notes asked. No sort-toggle buttons in the headers since sorting here was never user-driven (the original had no `onSortStatusChange` either — the DataTable's own default was always `createdAt desc`, computed once in JS before slicing to the current page, unchanged).
- **`Switch` → shadcn `switch.tsx`**, `variant="labeled"` `size="sm"`. Dropped the original's custom `styles={(theme) => ({track: {...}, label: {...}})}` entirely rather than translating it: it recolored the track based on `record.enabled`, but the checkbox's own `checked` state was driven by `record.active` (per the file's own `@ts-ignore` comment noting the N8N API returns `active`, not `enabled`) — since `enabled` is never actually present on the fetched records, that custom styling was dead/always-false in practice, silently decoupled from the switch's real checked state. shadcn's `variant="labeled"` drives its track color off the same `checked` prop the thumb position already uses, which is strictly more correct than what shipped before, not a fidelity compromise.
- **Deleted the dead `useEffect` that DOM-patched `.n8n_workflows_table`'s `aria-expanded`/`aria-haspopup`/`.mantine-datatable-empty-state` styling/SVG `aria-hidden`** (same treatment as `ProjectFilesTable.tsx`'s equivalent effect) — no popover or Mantine-generated empty-state markup remains to patch; the new `<Table>` has real `aria-label`s, a plain "No records found" cell, and native `<svg>` icons already carrying `aria-hidden="true"` from their icon component.
- Removed the file-level `/* eslint-disable @typescript-eslint/ban-ts-comment */` and the one `@ts-ignore` it existed for — the `active`-vs-`enabled` field-name mismatch is now handled with a plain type-narrowing cast (`(record as { active?: boolean }).active`) instead, so no lint suppression is needed anymore.
- Removed dead state that predated this conversion and had nothing to do with Mantine: `isWideScreen`/its `resize` listener (computed but never read outside its own effect — the one place that would have used it, `w={isWideScreen ? '85%' : '92%'}`, was already commented out) and `dataTableStyle` (an object with every property commented out). Also dropped `isSuccess`/`isErrorTools` from the `useFetchAllWorkflows()` destructure — both unused.
- Checked all four recurring failure modes: no absolute-positioned badge/indicator; no DOM-size measurement via ref+effect at all (nothing in this file measures its own layout); no `flex-col`+`items-center` anywhere in the file; no element combining a JSX `style` prop with an imperative `.style` write. All confirmed absent.

Verification: `grep @mantine|mantine-datatable` → 0 hits. Typecheck clean project-wide. `N8nWorkflowsTable.test.tsx` — fully rewritten against the real markup (the old file entirely mocked away `mantine-datatable`'s `DataTable`, which no longer exists to mock): now renders `PAGE_SIZE + 1` records to genuinely exercise 2-page pagination through the real `TablePaginationFooter`, asserts the switch toggle still calls `mutate({id, checked})` and surfaces the error toast, and adds a second test for the loading state (`role="status"` from the shared `LoadingSpinner`) — 2/2 pass (1 original scenario expanded + 1 new). `N8NPage.test.tsx`/`N8NPage.additional.test.tsx` (render this component) — 19/19 pass, unchanged. Full 6-shard smoke suite green.

**This completes Step 4a — the `ProjectFilesTable`/`N8nWorkflowsTable` mantine-datatable build-out — in full.**

**Step 4 cross-file reconciliation** (the follow-up `ProjectFilesTable.tsx`'s notes flagged): both parallel tasks landed, so `showToastOnUpdate`'s dead leading `theme` parameter is now dropped from `MakeQueryAnalysisPage.tsx` and both call sites (its own, and `ProjectFilesTable.tsx`'s `undefined as any` + explanatory comment) were updated to the 3-argument form. `was_error`/`isReset` are also unused in the body but were left in place — they're a pre-existing shape unrelated to the Mantine retirement, and every caller passes them explicitly.

Also fixed two defects in the in-flight `MakeQueryAnalysisPage.test.tsx`: (1) four `user.click(el, { pointerEventsCheck: 0 })` calls passed a per-call options object that `@testing-library/user-event` v14's `click` doesn't accept — typecheck errors (vitest doesn't typecheck, so the suite was green while `tsc` was not). The option was already set on that test's `userEvent.setup({ pointerEventsCheck: 0 })`, so the per-call copies were redundant and were removed rather than relocated. (2) The custom-date-range test ended in `expect(true).toBe(true)` — it clicked through the whole popover flow and then asserted nothing. Replaced with a real assertion that the trigger stops showing the "Pick date range" placeholder and renders `formatDateRangeLabel`'s `"Mon D – Mon D"` output, which is the conversion's own new behavior.

Verification after reconciliation: `tsc --noEmit` reports zero errors across all six migrated/new files (the remaining project-wide errors are pre-existing, in unrelated `src/utils/**` test files). `MakeQueryAnalysisPage.test.tsx` 7/7, `ProjectFilesTable.test.tsx` 5/5, `N8nWorkflowsTable.test.tsx` 2/2, `LargeDropzone.test.tsx` 39/39, `DocGroupMultiSelect.test.tsx` 4/4 — 50/50 across the four files run together.

**Remaining before Mantine can be uninstalled** (Step 5 teardown — no product component imports Mantine any more): `src/pages/_app.tsx`'s `MantineProvider`, `src/test-utils/renderWithProviders.tsx`'s `MantineProvider`, three now-dead `vi.mock('@mantine/core'|'mantine-datatable', …)` blocks (`axe-audit.a11y.test.tsx`, `smokeAllComponents.shared.tsx`, `ChatNavbar.test.tsx`/`ChatNavbar.a11y.test.tsx`), the `.mantine-*` selector blocks in `globals.css` (including the `@layer` ordering preamble that exists only because Mantine injects unlayered emotion styles at runtime), and the ten `@mantine/*` + `mantine-datatable` entries in `package.json`. Comment-only references (`use-media-query.tsx`, `toastUtils.ts`, the `globals.css` heading-scale note) are documentation of what was replaced and should stay.

**`ProjectFilesTable.tsx` visual-parity fix (post-conversion review against a screenshot of the Mantine original):** mantine-datatable rendered this table with a full grid (`withBorder` + `withColumnBorders`) and a filled header row; the shared shadcn `<Table>` draws only row separators on the default `border` color, so the converted table lost every vertical divider and its header fill, leaving the header visually merged into the tab strip above it. Fixed at the call site, not in `table.tsx` — that component backs every other table in the app, so widening its defaults would have been a much larger blast radius for a single table's fidelity. Added a module-level `TABLE_GRID_CLASSES` (`[&_th:not(:last-child)]:border-r` / `[&_td:not(:last-child)]:border-r`, plus a `border-(--table-border)` variant for each of `tr`, `th` and `td`) applied to both the main and error-state `<Table>`s, switched the sticky `TableHeader` from `bg-(--background)` to `bg-(--table-header-background)` (the same token the old `.project_files_table .mantine-Table-root thead th` rule in `globals.css` used, so the color is carried over rather than re-picked), and added `mt-2` to the table container for the gap the original had between the tab strip and the table's top border.

Two further differences from the original are recorded but **not** changed, since they weren't part of the reported issue: the original striped alternate body rows (mantine-datatable's `striped`), and the active tab's fill is inverted relative to the original (original: white/raised tab against a gray table header; current: gray tab against what was a white header — now a gray one, so the tab and header share a color across the new 8px gap).

**`ProjectFilesTable.tsx` visual-parity, round 2:**

- **Compact filter header restored.** `FilterInput` (an always-visible input under every column label, which doubled the header's height) became `FilterPopover` — a funnel-icon `PopoverTrigger` holding the same input, so the header is one compact row as in the original. The trigger takes the `Filter by <column>` accessible name and turns `--dashboard-button`-colored when that column's filter is set; the input inside keeps the plain column label, so `getByLabelText('File Name')` still resolves to the input and `getByRole('button', {name: /^File Name/})` still resolves to the sort control.
- **Active tab is white/raised again** (`bg-(--background)` + a `--table-border` outline) instead of `--table-header-background`, which now belongs to the table header it used to borrow. Inactive tabs carry `border border-transparent` so the two states stay the same size. A closed box rather than the open-bottomed folder-tab shape, since there is now a deliberate gap between the tab strip and the table.
- **Row striping needed no change** — it was already implemented as `index % 2 === 0 ? 'bg-(--background)' : 'bg-(--background-faded)'` on the body `TableRow`, using the same token as the header fill. It had been reported as missing based on a screenshot; the rendered DOM in the test output confirms the class is applied.

Two test-infra notes from this round, both about Base UI popups making the rest of the page `aria-hidden` while open, which hides it from `getByRole` (but _not_ from `getByLabelText` — which is why the pre-existing `getByLabelText('File Name')` filter step never tripped over it): the document-group combobox opened earlier in the test now needs an explicit `{Escape}` before the header can be queried by role, and the filter popover itself needs one before the sort button can be clicked.

Verification: typecheck clean for this file; `ProjectFilesTable.test.tsx` 5/5; `npm run test:a11y` 15 files / 60 tests pass.

**"Select Group" combobox focus outline.** The orange ring around the document-group combobox was not from the conversion — it is the app-wide keyboard-accessibility outline (`*:focus-visible { outline: … var(--illinois-orange) !important }`, declared twice in `globals.css`, at the `@layer` preamble and again under "Global focus-visible styles"). Under Mantine the control drew no ring of its own, so the orange one was the only indicator; the shadcn combobox adds `focus-within:border-ring` plus a 3px `ring-ring/50`, which stacked a second ring outside the first. Added a `.doc-group-multiselect` class (applied by `DocGroupMultiSelect` itself, merged ahead of any caller `className`) and a `globals.css` rule suppressing the outline on the input group and its text input only. Scoped deliberately: the chips' remove buttons have no ring of their own, so they keep the global orange outline, and the control still has a visible focus indicator — this removes a duplicate ring, not the only one. The rule is more specific than `*:focus-visible`, so it wins regardless of declaration order.

**Alternate row shading was genuinely broken — root cause was in the shared `table.tsx`, not `ProjectFilesTable.tsx`.** The striping classes (`index % 2 === 0 ? 'bg-(--background)' : 'bg-(--background-faded)'`) were present and correct all along, which is why the earlier report that "striping is already implemented" looked right from the source. Sampling the pixels of a screenshot of the running app settled it: all three body rows were exactly `rgb(248,248,248)` — identical, and matching neither token (the header sampled `rgb(227,231,234)`, i.e. `--background-faded` exactly, which validates the method).

The cause: `TableRow`'s base class list contains `has-aria-[expanded]:bg-muted/50`, which Tailwind compiles to `:has([aria-expanded])` — an _attribute-presence_ match, not `="true"`. Every row of this table contains the "Select Group" combobox, whose input always carries `aria-expanded="false"`, so **every** row matched. `:has()` contributes its argument's specificity, making that selector (0,2,0) against the striping utilities' (0,1,0), so it won outright and painted every row `var(--muted)` (`--illinois-storm-95`, `#f4f4f4`).

Fixed in `src/components/shadcn/ui/table.tsx` by changing the variant to `has-[[aria-expanded=true]]:bg-muted/50`, which is what the rule was obviously reaching for — a row is highlighted when something inside it is _expanded_, not merely when it contains an expandable control. Fixing it at the source rather than forcing `!important` on this one table's stripes: the same latent bug would flatten the rows of any other table containing a combobox, select, popover trigger or disclosure button, and an `!important` stripe would also have killed the row hover highlight.

Verification: full suite green — 290 files, 2468 tests (up from 289/2460; the new `DocGroupMultiSelect.test.tsx` and the extra `LargeDropzone`/`N8nWorkflowsTable` cases account for the delta). Typecheck clean for both changed files. Still unverified in a browser this session.

**`SimPage.tsx`** (546 lines, `@mantine/core`'s `Badge`/`Button`/`Card`/`Flex`/`Group`/`Stack`/`Table`/`Text`/`TextInput`/`Title` plus `@mantine/hooks`' `useMediaQuery`). This file was never in the plan — it arrived with main's `efd67621 "feat: replace n8n with sim"`, which deleted `N8NPage.tsx`/`N8nWorkflowsTable.tsx` and replaced them with a Mantine-based page. With it converted, **no product component imports Mantine any more**; only the provider, the test harness, dead test mocks, dead CSS and `package.json` remain.

- `TextInput label/description/error` → `Label` + `Input` + a description `<p>` + a conditional error `<p>`, wired by hand with `htmlFor`/`id` and `aria-describedby` (both ids when an error is showing), plus `aria-invalid` on error. Mantine generated all of that association automatically; the shadcn primitives do not, and losing it would have silently dropped the labels from the accessibility tree. The new test asserts `getByLabelText` resolves all three inputs precisely to lock that in.
- `Table striped highlightOnHover` → shadcn `<Table>` with the `index % 2` striping idiom established in `ProjectFilesTable.tsx`. Note this table has no combobox in its rows, so it never hit the `has-[[aria-expanded=true]]` bug fixed earlier — but it uses the same fixed `TableRow`, so the striping works from the start.
- `Card withBorder padding="none" radius="xl"` → shadcn `Card` with `rounded-4xl border p-0 shadow-none ring-0` and the same inline `backgroundColor`/`borderColor` style object, mirroring `DocumentsCard.tsx`'s already-converted admin card exactly rather than re-deriving a radius from Mantine's scale.
- `Group spacing="lg" m="1rem"` → `m-4 flex flex-wrap items-center gap-4`; `Stack align="start"` → `flex flex-col items-start`; `Flex direction="column" align="center" w="100%"` → `flex w-full flex-col items-center` (the genuine-centering case again — the child carries its own width through `cardWidthClasses`).
- `Title order={2|3|5}` → `<h2|h3|h5>` + `.heading-h2|h3|h5`; `Text` → `<p>` with `size="xs"`→`text-xs`, `size="sm"`→`text-sm`, `c="dimmed"`→`text-(--foreground-faded)`, `color="red"`→`text-(--error)`, `weight={500}`→`font-medium`, `lineClamp={2}`→`line-clamp-2`, `mt="xs"`→`mt-2.5`.
- `Badge color="yellow" variant="outline" leftSection={<IconAlertCircle/>}` → shadcn `Badge variant="outline"` with the icon as a leading child and `gap-1 border-amber-500 text-amber-600` (shadcn's `outline` variant has no color prop).
- `useMediaQuery` from `@mantine/hooks` → the local `@/components/shadcn/hooks/use-media-query`, which already matches Mantine's false-until-mount behavior.
- Checked the four recurring failure modes: no absolute-positioned badge/indicator; no ref-measured DOM sizing anywhere in the file; the one `items-center` is the genuine-centering case above; no element combines a JSX `style` prop with an imperative `.style` write. The two remaining inline `style` objects (card colors, the conditional `borderLeft`) were kept as-is — they read CSS variables that have no Tailwind token, exactly as the original did.

New `SimPage.test.tsx` (2 tests): the config form renders with all three inputs label-associated and the stored-key mask, routing badge and workflows table populated; and a failing save surfaces the server's error message through an error toast — that second one guards the `!upsertRes.ok` check, since `fetch` does not reject on 4xx and the success toast sits directly below it.

Verification: `grep @mantine` → 0. `npm run typecheck` clean. Full suite 290 files / 2599 tests pass. `npm run test:a11y` 60/60.

## Step 5 — teardown (Mantine uninstalled)

- **`_app.tsx`** — `MantineProvider` and its whole theme object removed. Nothing depended on the theme: the heading scale it defined was already ported to `.heading-h1`–`.heading-h6` in `globals.css`, and the `colors` map only aliased CSS variables that are read directly everywhere else.
- **`renderWithProviders.tsx`** — `MantineProvider` dropped from the test wrapper; `ThemeProvider` now wraps `HomeContext` directly.
- **Five dead test mocks removed** — `@mantine/core`'s `Burger`/`Transition` stubs in `axe-audit.a11y.test.tsx`, `ChatNavbar.test.tsx` and `ChatNavbar.a11y.test.tsx`, the `mantine-datatable` `DataTable` stub in `smokeAllComponents.shared.tsx`, and a `MantineProvider` wrapper in `__tests__/pages/__tests__/landingPage.test.tsx`. That last one lives in the top-level `__tests__/` tree rather than `src/`, so every earlier `grep` in this document — all of which were scoped to `src/` — missed it; it only surfaced when the package was physically uninstalled and vitest failed to resolve the import. Worth remembering that this repo has two test roots.
- **`globals.css`** — all remaining `.mantine-*` rules deleted (Modal reflow, the `.project_files_table` datatable hacks, the form-label contrast rule). Comment-only references elsewhere were kept: they document what replaced what.
- **`package.json`** — the nine `@mantine/*` packages, `mantine-datatable` and `@emotion/react` all uninstalled.

**`clsx` was a phantom dependency.** `src/components/shadcn/lib/utils.ts` imports it directly, but it was never declared — it only existed in `node_modules` as a transitive of Mantine/emotion. Uninstalling Mantine took it with it and broke every shadcn component at once. Now declared explicitly. `tailwind-merge` and `class-variance-authority`, the other two `cn`/variant deps, were already declared correctly.

**`npm uninstall` left `node_modules` in a broken state** (`vitest/config` unresolvable, then `ENOTEMPTY` on `node_modules/next` for every repair attempt). `npm ci` fixed it. Side effect worth knowing: this killed the dev server that was running on :3000.

### The `@layer` restoration — attempted, reverted, still open

Preflight and the utilities **remain unlayered**. Re-layering them was attempted as the final teardown step and **visibly broke the app**; the change was reverted after a browser check.

The reasoning behind the attempt was that the unlayered imports existed only as a Mantine workaround, so removing Mantine should let them go back to `layer(base)`/`layer(utilities)`. That is half right. What it missed: this file's own ~40KB of custom rules (`.think-tag-*`, `.chat-input-container`, `.project_files_table`, the `body` rules, and many more) are themselves unlayered. While the utilities are also unlayered, those custom rules and the utilities compete on ordinary specificity — which is how the app has always behaved, in v3 and after the v4 codemod. Move the utilities into `layer(utilities)` and every one of those unlayered custom rules wins against every utility on the same element regardless of specificity. That is a silent, app-wide restyling that no test in this repo can catch, because jsdom does not implement the cascade.

Worth recording precisely, because the build output looked _correct_ while the app looked wrong: after the change, `@layer utilities` held 199KB with `.border-border` inside it, `@layer base` held preflight plus both compat blocks, and only the custom rules were left unlayered. Every structural check passed. The regression was purely one of cascade precedence between two groups of rules that were both present and both correct in isolation. **A green build, a green suite and a correct-looking layer structure are not evidence that a cascade change is safe — only looking at the running app is.**

One detail for whoever picks this up: re-layering the imports alone is not enough even in principle. `* { @apply border-border }` and `body { @apply bg-background text-foreground }` sit unlayered at the bottom of this file; layer the utilities and leave those alone and `*` beats every `border-*` utility in the app. They have to move into `@layer base` in the same change (that is where PR #196 took them from). That part was done correctly during the attempt and is not what broke it.

Re-layering is still the right end state. It is a separate piece of work: audit the custom rules in this file, layer them deliberately, and verify in a browser page by page.

Verification of the teardown as it now stands: `grep @mantine` → comments only. `npm run typecheck` clean. Production build succeeds. Full suite 290 files / 2599 tests pass. The compiled CSS has no `@layer utilities` block, and `.border-border` and `body`'s `!important` declarations are unlayered — i.e. the arrangement that works.
